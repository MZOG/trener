import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dla trenera'
};

const ForTrainerPage = () => {
  return (
    <section className="max-w-6xl mx-auto px-5">
      <p>ForTrainerPage</p>
    </section>
  );
};

export default ForTrainerPage;
