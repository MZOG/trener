import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dla trenującego'
};

const ForTraineePage = () => {
  return (
    <section className="max-w-6xl mx-auto px-5">
      <p>ForTraineePage</p>
    </section>
  );
};

export default ForTraineePage;
