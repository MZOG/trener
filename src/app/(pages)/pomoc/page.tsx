import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pomoc'
};

const FAQ = () => {
  return (
    <section className="max-w-6xl mx-auto px-5">
      <p>Faq</p>
    </section>
  );
};

export default FAQ;
